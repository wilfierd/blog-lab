*.png
*.drawio

# Secrets & local env
.env
*.tfvars
*.tfvars.json

# Terraform runtime (never commit)
**/.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# Keep: .terraform.lock.hcl (pins provider versions — should be committed)
*.md