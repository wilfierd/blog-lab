*.png
*.drawio

# Secrets & local env
.env
*.tfvars
*.tfvars.json
secrets-values.yaml

# Terraform runtime (never commit)
**/.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# Keep: .terraform.lock.hcl (pins provider versions — should be committed)
*.md
monitoring-local/logs/blog-app.log
blog-local
blog-app-bin
blog-local
monitoring-local/prometheus.yml.template
monitoring-ec2/alertmanager.yml
