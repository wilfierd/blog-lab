node_exporter_version: "1.8.2"
node_exporter_port: 9100

# Private IP of the monitoring EC2 in the VPC — from `terraform output monitoring_private_ip`
# Set at deploy time: -e loki_push_ip=10.0.x.x
loki_push_ip: ""
loki_port: 3100
